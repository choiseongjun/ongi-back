apiVersion: v1
kind: Service
metadata:
  name: {{ include "ongi-back.fullname" . }}
  labels:
    {{- include "ongi-back.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ongi-back.selectorLabels" . | nindent 4 }}
